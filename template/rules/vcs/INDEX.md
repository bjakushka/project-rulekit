## Version control

- Don't run repo operations yourself (commit, push, merge, rebase, branch or tag changes). 
  Delegate them to me unless I've explicitly said you're doing this one
- Read-only inspection (status, diff, log, show) is fine without asking
- Before any repo operation, check the current directory and repo status

@layout.md
